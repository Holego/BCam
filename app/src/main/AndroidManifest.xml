<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Required from Android 14 for a service declaring foregroundServiceType="camera". -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <!-- Same, for the "microphone" type, which is only requested when audio is enabled. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Android 13+: without it the ongoing recording notification is simply not shown. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- PARTIAL_WAKE_LOCK held only while a recording is active; see RecordingService. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
        No WRITE_EXTERNAL_STORAGE: all output goes through the Storage Access Framework
        folder the user picks, which needs no storage permission on any API level.
    -->

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BackgroundCamera">

        <!--
            Lets AppCompat persist the in-app language choice on Android 12 and below.
            The service is never started; it exists purely to carry the meta-data.
        -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AboutActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!--
            Owns the camera and the active recording. Started with startForegroundService()
            from the Activity, and directly by the widget's / notification's PendingIntent.
        -->
        <service
            android:name=".recording.RecordingService"
            android:exported="false"
            android:foregroundServiceType="camera|microphone" />

        <receiver
            android:name=".widget.RecordingWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_record_info" />
        </receiver>
    </application>
</manifest>
